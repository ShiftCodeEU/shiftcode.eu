---
import "../styles/tailwind.css";

import i18next, { t, changeLanguage } from "i18next";

import LanguageSelector from "@components/i18n/parts/languageSelectbox";
import Layout from "@components/layouts/MainLayout.astro";

const supportedLanguages = i18next.languages;

changeLanguage("en");
---

<Layout title={t("site.title")} description={t("site.desc")}>
	<div class="flex flex-row">
		<span class="mt-2 font-extrabold">
			{t("home.welcome")}
		</span>
		<div class="ml-auto mr-0">
			<LanguageSelector
				showFlag={true}
				langsAvailable={supportedLanguages}
				currentLang={i18next.language}
				client:only
			/>
		</div>
	</div>
</Layout>
