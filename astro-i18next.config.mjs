/** @type {import('astro-i18next').AstroI18nextConfig} */
export default {
  defaultLocale: "en",
  locales: [
    "en", // The `defaultLocale` value must present in `locales` keys
    "de",
    "fr",
    "es",
    "nl",
  ],
};
